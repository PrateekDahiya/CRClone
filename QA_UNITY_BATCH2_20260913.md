# QA Unity Batch #2 — 2026-09-13 - `feature/qa-integration`

Source: `unityErrors2.md` (746 lines, committed). Packages resolved on disk during this batch
(lockfile + PackageCache confirm ugui/inputsystem/tmp/addressables/test-framework/nunit).

## Key finding: batch#2 is mixed-era — part already clearing
ugui errors (~100: Text/Image/Button/EventSystems/Selectable/Graphic) are GONE while
TMPro/InputSystem/Addressables/ProtoBuf persist → user copied mid-resolution. The package
half needs NO code: finish resolution + Editor restart (inputsystem backend), then recompile.
`protobuf-net.dll.meta` + `Plugins.meta` generated by Editor and committed.

## Fixes landed (4 branches, file-disjoint, merged)
| Branch | Content | Commit |
|---|---|---|
| `feature/qa-fix-u2nest` | `ClanScreen.X`/`ShopScreen.X` false-nesting → bare names + `using CRClone.UI.Screens` (5 files). STOP: `OfferType` doesn't exist → see below | `e8de439` |
| `feature/qa-fix-u2using` | 8 added usings (Collections/Core/UI/Components). NOTE: `ScreenType` is nested in `EventBus`, so bare refs NOT fixed by usings → see below | `913bd53` |
| `feature/qa-fix-u2bugs` | ShopScreen +`IEnumerator` using; `PlayerData` DTO defined + `PlayerLocalData : PlayerData`. STOPs: dup class, dup method (below) | `6a3ed01` |
| `feature/qa-fix-u2decide` | `ScreenType` → namespace level (zero qualified refs, safe); `OfferItemUI`↔`ShopOffer` alignment (`RewardType`, +4 fields); `ProfileBattleLogItemUI` rename; `OnDrag` event → `OnDragged`; OfferItemUI Core/Data usings | `947bc3c`+`fa8af0b` |

## STOP resolutions (QA-decided from evidence)
1. `OfferType` (6 refs): does not exist; `RewardType` values identical → OfferItemUI uses `RewardType`, ShopOffer gains `purchased/timeRemaining/goldAmount/gemAmount`.
2. `ScreenType` bare refs (~40): moved enum to namespace level (no `using static` convention in repo).
3. `BattleLogItemUI` dup: ProfileScreen's copy → `ProfileBattleLogItemUI` (distinct DTOs per screen).
4. `OnDrag` event-vs-method: event → `OnDragged` (zero subscribers; method keeps IDragHandler name).
5. ReconnectionManager `BattleData`/event errors: STALE (types exist, NetworkClient's identical refs clean).

## New/anticipated (not fixed — needs next compile or design)
- ISSUE-104/105 (PlayerState dual-namespace, FixedVector2 dup struct) still open.
- `EventBus.On<T>/Off<T>` generics called at ReconnectionManager:38-47 don't exist → EXPECTED in next batch.
- Unity Console truncates long lists: batch files are non-exhaustive; unlisted member errors (e.g. PlayerLocalData.*) remain possible.
- `/health degraded`-on-memory, dual DB pools: prior notes stand.

*QA Agent 7 — fixes by directed agents, diffs reviewed on merge; compile proof belongs to Unity.*
